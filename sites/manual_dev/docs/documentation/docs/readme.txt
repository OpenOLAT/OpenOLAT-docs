This directory is here only for compatibility. 


The index file embeds the README.md from the project itself which references the 
OpenOlat image form a docs/assests/ directory. This must be like this to make the 
image appear in github. The directory and the image in this directory on the other
hand is for the publication in docs.openolat.org